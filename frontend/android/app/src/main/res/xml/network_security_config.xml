<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configuration de sécurité réseau pour TechPlus -->
    
    <!-- Configuration par défaut -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs -->
            <certificates src="system"/>
            <!-- Trust user added CAs -->
            <certificates src="user"/>
        </trust-anchors>
    </base-config>

    <!-- Configuration pour le domaine de production -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.techplus.com</domain>
        <domain includeSubdomains="true">staging-api.techplus.com</domain>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
        <!-- Certificate pinning pour la production -->
        <pin-set expiration="2025-12-31">
            <!-- Remplacez par les vrais hashes de certificats -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
    </domain-config>

    <!-- Configuration pour le développement local (debug uniquement) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.1.1</domain>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </domain-config>

    <!-- Configuration pour les tests -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
</network-security-config>
